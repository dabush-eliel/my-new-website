import React from "react";
import ListItems from "../../components/lists/list-items";

function OtherExperienceList({ items }) {
    return <ListItems items={items} title="Others" />;
}

export default OtherExperienceList;
